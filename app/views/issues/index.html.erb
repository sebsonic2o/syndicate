<div class="issues">
  <% @issues.each do |issue| %>
    <div id="issue-<%= issue.id %>" class="issue">

      <div class="issue-body">
        <div class="issue-image">
          <%= image_tag(issue.image_url) %>
        </div>
        <h2 class="issue-heading"><%= link_to issue.title, issue %></h2>
        <p><%= issue.description %></p>
      </div>
  </div>
  <% end %>
</div>

